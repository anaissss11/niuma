const TARGET=510;function k(){return new Date().toDateString()}function reset(){if(localStorage.day!==k()){localStorage.day=k();localStorage.removeItem("start");localStorage.removeItem("end")}}function fmt(t){return new Date(t).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit",hour12:false})}function saveHist(){if(!localStorage.start||!localStorage.end)return;let h=JSON.parse(localStorage.hist||"[]");let d=Math.floor((localStorage.end-localStorage.start)/60000);h.unshift({day:new Date().toLocaleDateString(),s:fmt(localStorage.start),e:fmt(localStorage.end),m:d});localStorage.hist=JSON.stringify(h)}function clockIn(){if(localStorage.start)return;localStorage.start=Date.now();render()}function clockOut(){if(localStorage.end)return;localStorage.end=Date.now();saveHist();render()}function pick(cb){let p=picker;p.onchange=function(){let a=this.value.split(':');let d=new Date();d.setHours(+a[0],+a[1],0,0);cb(d.getTime())};p.showPicker?p.showPicker():p.click()}function editStart(){pick(v=>{localStorage.start=v;render()})}function editEnd(){pick(v=>{localStorage.end=v;saveHist();render()})}function render(){reset();let s=localStorage.start?+localStorage.start:null,e=localStorage.end?+localStorage.end:null;startTime.textContent=s?fmt(s):'--:--';endTime.textContent=e?fmt(e):'--:--';expected.textContent=s?fmt(s+30600000):'--:--';let m=s?Math.floor(((e||Date.now())-s)/60000):0;prog.value=Math.min(m,TARGET);if(m>=TARGET){status.textContent='âœ… å¯ä»¥ä¸‹ç­å•¦';prog.className='donebar';}else{status.textContent='ðŸšœ Keep Farming';prog.className='';}if(s){inBtn.textContent='âœ“';inBtn.disabled=true;inBtn.classList.add('done')}if(e){outBtn.textContent='âœ“';outBtn.disabled=true;outBtn.classList.add('done')}let h=JSON.parse(localStorage.hist||'[]');history.innerHTML=h.map(x=>x.day+'<br>'+x.s+' â†’ '+x.e+'<br>'+Math.floor(x.m/60)+'h '+(x.m%60)+'m<hr>').join('')||'No Records'}render();setInterval(render,30000);