const WORK_HOURS=8.5;function saveHistory(s,e){const h=JSON.parse(localStorage.getItem('history')||'[]');const d=Math.round((e-s)/60000);h.unshift({day:new Date().toLocaleDateString(),start:new Date(s).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}),end:new Date(e).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}),dur:Math.floor(d/60)+'h '+(d%60)+'m'});localStorage.setItem('history',JSON.stringify(h));}function clockIn(){if(localStorage.start)return;localStorage.start=Date.now();render()}function clockOut(){if(!localStorage.start)return;saveHistory(Number(localStorage.start),Date.now());localStorage.done='1';render()}function modifyStartTime(){const v=prompt('Enter clock-in time (HH:MM)','08:30');if(!v)return;const a=v.split(':');const n=new Date();n.setHours(+a[0],+a[1],0,0);localStorage.start=n.getTime();render()}function render(){const st=localStorage.start;if(st){const d=new Date(Number(st));startTime.textContent=d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});leave.textContent=new Date(Number(st)+WORK_HOURS*3600*1000).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});clockInBtn.textContent='✓ CLOCKED IN';clockInBtn.classList.add('disabled');}if(localStorage.done){clockOutBtn.textContent='✓ CLOCKED OUT';clockOutBtn.classList.add('disabled');}const h=JSON.parse(localStorage.getItem('history')||'[]');historyList.innerHTML=h.map(x=>`<div>${x.day}<br>${x.start} → ${x.end}<br>${x.dur}</div><hr>`).join('')||'No Records';}render();